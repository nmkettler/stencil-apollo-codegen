# mutation upvotePost($postId: Int!) {
#   upvotePost(postId: $postId) {
#     id
#     votes
#   }
# }

mutation agentOnlineStatus($postId: Int!) {
  updateStatus(postId: $postId) {
    name
    agent_online
  }
}