import { EventEmitter } from "../../stencil.core";
import { DocumentNode } from "graphql";
import { MutationRenderer, MutationFn } from "../../utils/types";
import { ApolloClient, MutationOptions } from "apollo-client";
export declare class ApolloMutationComponent {
    mutation: DocumentNode;
    renderer: MutationRenderer;
    variables: any;
    options: MutationOptions;
    mutationFn: MutationFn<any, any>;
    client: ApolloClient<any>;
    el: HTMLApolloMutationElement;
    readyEventEmitter: EventEmitter<MutationFn<any, any>>;
    componentWillLoad(): void;
    onPropsChange(): void;
    passMutation(): Promise<void>;
    render(): LocalJSX.Element | LocalJSX.Element[];
}
