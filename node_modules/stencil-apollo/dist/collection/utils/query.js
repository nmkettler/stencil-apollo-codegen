import { h } from "@stencil/core";
export const Query = ({ query, variables, options, }, children) => h("apollo-query", { query: query, variables: variables, options: options, renderer: children[0] });
