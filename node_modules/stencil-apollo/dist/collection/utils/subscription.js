import { h } from "@stencil/core";
export const Subscription = ({ subscription, variables, options, }, children) => h("apollo-subscription", { subscription: subscription, variables: variables, options: options, renderer: children[0] });
