import { h } from "@stencil/core";
export const Mutation = ({ mutation, variables, options }, children) => h("apollo-mutation", { mutation: mutation, variables: variables, options: options, renderer: children[0] });
