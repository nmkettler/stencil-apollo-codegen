import { Compiler, Config, DevServer, ITesting } from '@stencil/core/internal';
import * as puppeteer from 'puppeteer';
export declare class Testing implements ITesting {
    isValid: boolean;
    compiler: Compiler;
    config: Config;
    devServer: DevServer;
    puppeteerBrowser: puppeteer.Browser;
    constructor(config: Config);
    runTests(): Promise<boolean>;
    destroy(): Promise<void>;
}
